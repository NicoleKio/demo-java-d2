spring:
  application:
    name: demo-java-d2
  profiles:
    active: store1
  datasource:
    url: "jdbc:postgresql://localhost:5432/XXX"
    username: "postgres"
    password: "postgres"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  data:
    mongodb:
      uri: mongodb+srv://yuumon1:yuu@cluster0.re97kzg.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: yuu-db
